const styles = theme => ({
    "@global": {
        body: {
            backgroundColor: '#fff'
        }
    },
    paper: {
        marginTop: '50%',
        display: "flex",
        flexDirection: "column",
        alignItems: "center"
    },
    submit:{
        color:'#fff',
        backgroundColor:'#00C170'
    }
});

export default styles;
