const styles = theme => ({
    "@global": {
        body: {
            backgroundColor: '#fff'
        }
    },
    paper: {
        marginTop: '50%',
        display: "flex",
        flexDirection: "column",
        alignItems: "center"
    },
    title: {
        textAlign:'center',
        marginBottom: "5px"
    },
    submit:{
        color:'#fff',
        backgroundColor:'#00C170'
    }
});

export default styles;
